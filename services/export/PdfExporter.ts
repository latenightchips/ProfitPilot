/**
 * PDF Exporter — 04_BUILD_GUIDE.md's own "IMPORT / EXPORT DIRECTORY" lists
 * this file alongside `JsonExporter.ts`/`CsvExporter.ts`, but no task in
 * M8-036–M8-045 requires PDF output and no PDF-generation library exists
 * in this project's dependencies. Intentionally left as an empty stub,
 * matching the same "Future PDF" deferral language the 4 existing feature
 * exporters already use for the same reason.
 */
export {};
