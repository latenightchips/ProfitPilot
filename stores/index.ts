/**
 * Global application state — public entry point.
 *
 * 04_BUILD_GUIDE.md "STORES DIRECTORY": "Global application state,"
 * naming `PortfolioStore` as its own first example. `06_TASKS.md`
 * M4-003 ("Implement Portfolio Store") is its first occupant.
 */
export * from './portfolioStore';
