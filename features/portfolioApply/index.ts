/**
 * Portfolio Apply feature module — public entry point. V1.1 Batch 3
 * ("Apply to Portfolio"). `ApplyToPortfolioReview` is the shared
 * confirmation panel every apply trigger component
 * (`features/loop-builder/components/ApplyLoopToPortfolio.tsx`,
 * `features/exit-planner/components/ApplyExitPlanToPortfolio.tsx`,
 * `features/simulation/components/ApplySimulationToPortfolio.tsx`)
 * renders — see that component's own header comment.
 */
export { ApplyToPortfolioReview } from './components/ApplyToPortfolioReview';
